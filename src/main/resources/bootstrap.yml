spring:
  application:
    name: GATEWAY-SERVER
  cloud:
    nacos:
      discovery:
        serverAddr: 127.0.0.1:8848
        namespace: public
        group: ${spring.application.name}
      config:
        group: ${spring.application.name}
        namespace: public
        serverAddr: 127.0.0.1:8848
#        extensionConfigs:
#          - data-id:
#            group:
#            refresh:
#          - data-id:
#              group:
#              refresh:
server:
  port: 10001
